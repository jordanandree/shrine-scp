#!/usr/bin/env ruby

require "bundler/setup"
require "shrine/storage/linter"
require "shrine/storage/rsync"

storage = Shrine::Storage::Rsync.new(directory: "./tmp")
Shrine::Storage::Linter.new(storage).call
